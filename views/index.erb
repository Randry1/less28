<h2>Lates posts:</h2>
<div class="row">
  <div class="col-md-12">

    <% @table_posts.each do |row| %>

        <p class="text-success">
          <%= row['content'] %>
        </p>
        <p>
          Add at:<%= row['create_date'] %>
        </p>
        <a href="/details/<%= row['id'] %>">Comments</a>
        <br />
        <br />

    <% end %>

  </div>
</div>